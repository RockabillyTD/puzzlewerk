<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Regel S1: KEINE Permissions in Version 1. Jede neue Permission braucht
         ein ADR und menschliche Freigabe. -->

    <!-- Regel S2: Backups bewusst deaktiviert — allowBackup gilt bis Android 11,
         dataExtractionRules ab Android 12 (Cloud-Backup + Device-Transfer). -->
    <application
        android:name=".PuzzlewerkApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Puzzlewerk">

        <!-- Regel S5: Einzige exportierte Komponente ist die Launcher-Activity. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
